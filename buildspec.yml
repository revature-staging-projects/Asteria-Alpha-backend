version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto8
  pre_build:
    commands:
      - echo In the pre_build phase...
  build:
    commands:
      - echo Build started
      - mvn install
  post_build:
    commands:
      - echo Build completed
artifacts:
  files:
    - target/Asteria-Alpha-Backend-1.0-SNAPSHOT.jar
    - scripts/*
